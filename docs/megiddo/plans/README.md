# Shared Cursor / agent plans

Machine-local `~/.cursor/plans/` does not sync via Google Drive. Put plan files here so Mac and Linux share them through the ORK3 Insync checkout.

On each host, optionally symlink from `~/.cursor/plans/<name>.plan.md` to the file in this directory so Cursor’s plan UI still opens them.

