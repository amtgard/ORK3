# Worker — I-0 (Idiom charter)

```
You are executing **Megiddo I-0** only — idiom discovery and charter.

Read: docs/megiddo/refactor/skills/idiom-enforcement/workers/_shared-procedure.md, docs/megiddo/refactor/05-development-steering.md DS-1, docs/megiddo/refactor/12-idiom-enforcement.md, docs/megiddo/refactor/01-code-decomposition.md, docs/megiddo/refactor/04-milestone-checklist.md § R-01 … R-19d complete sections

| Field | Value |
|-------|-------|
| Branch | `megiddo/i-0-idiom-charter` |
| Stack base | `megiddo/p3-validate-20-audit` @ milestone-checklist.md (VALIDATE-20 ok prerequisite) |
| Scope | Documentation + lint commands — no production behavior changes |

## Tasks

1. Survey legacy reference files cited in `12-idiom-enforcement.md` plus 2–3 clean peers per layer (controller, model, AJAX).
2. Publish `docs/megiddo/refactor/idioms-00-charter.md` with:
   - §1 Rules — controller `load_model`, model wrappers, domain instantiation, JSON shapes, whitespace
   - §2 Anti-patterns — catalog drift from R-19* (e.g. `(new Model_Player())` in AJAX, inline `new Dangeraudit()` where file uses model path)
   - §3 Hop scope table — I-01…I-18: files from each `megiddo/r-{nn}-*` branch diff vs integration; I-19a…I-19d: fixed 3-file groups
   - §4 Lint commands — `rg` checks agents run at I-VALIDATE (e.g. `new Model_` in controllers, `Ork3::\$Lib`, `$DB->`)
   - §5 Gate map — which I-* hops rerun fuzzy/Playwright (copy from v-* / R-* workers)
3. Update `04-milestone-checklist.md` § Phase 3.5 stub rows with links.
4. Do not edit `orkui/` except optional comment-only fixes.

## Gates

```bash
sh bin/run-unit-tests.sh   # exit 0
```

Commit: `I-0: ORK3 idiom charter and hop scope for Phase 3.5.`  
Update `skills/idiom-enforcement/milestone-checklist.md`; return report.
```
