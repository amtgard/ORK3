# Pending PR reply — C-28 (comment id 3691046681)

Post on: https://github.com/amtgard/ORK3/pull/492#discussion_r3691046681

---

Restored master ordering for kingdom/park abbreviation-prefix player search: `ScopedPlayerSearch` now initializes `$orderClause` to `m.suspended ASC, m.active DESC, m.persona` before the `filterPid` / `filterKid` branches, so `KD:` and `KD:PK:` queries rank active members above inactive and suspended when those rows are included. `SearchServiceTest::testScopedPlayerSearchKdPrefixOrdersActiveBeforeInactive` covers both prefix forms.

Commit: `ea4a6608`
