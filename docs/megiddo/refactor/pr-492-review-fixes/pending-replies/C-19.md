# Pending PR reply — C-19 (comment id 3691046630)

Post on: https://github.com/amtgard/ORK3/pull/492#discussion_r3691046630

---

Fixed in `fix-pr-492`: removed the client-trusted `AuthorizedByController` bypass on `Event::RemoveRsvp`. All paths now require a session `Token` and authorize through `_authorizeRsvpActor` (self, event `AUTH_EDIT`, or attendance staff with `can_attendance`).

`Model_Event::remove_rsvp` and `Model_EventPlanning::remove_rsvp` pass the session token; `Controller_EventAjax::delete_rsvp` threads `$this->session->token`.

Integration tests cover: no token / `AuthorizedByController:true` without token → `SecureTokenFailure`; stranger denied; self and staff paths still succeed.
