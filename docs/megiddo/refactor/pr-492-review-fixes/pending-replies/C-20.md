# Pending PR reply — C-20 (comment id 3691046638)

Post on: https://github.com/amtgard/ORK3/pull/492#discussion_r3691046638

---

Fixed in `fix-pr-492`: `Event::GetRsvpList` now requires a session `Token` and gates on manage or attendance authority via `EventPlanning::CanManageEventDetail` (same surface as staff RSVP management).

Added SOAP `GetRsvpListRequest` (`Token` + `EventCalendarDetailId`); `Model_Event::get_rsvp_list` passes the session token (optional explicit token for tests).

Integration tests: anonymous / no token → `SecureTokenFailure`; stranger → `NoAuthorization`; event editor and attendance staff still receive the roster.
