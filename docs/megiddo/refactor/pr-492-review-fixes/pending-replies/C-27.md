# Pending PR reply — C-27 (comment id 3691046675)

Post on: https://github.com/amtgard/ORK3/pull/492#discussion_r3691046675

---

Restored master parity for park-scoped ladder grids: `Report::GetLadderAwardGrid` no longer looks up `park.kingdom_id` when the request is park-only (`ParkId` set, `KingdomId` 0). Column headers again come from global `award.is_ladder` rows, while kingdom-scoped requests still use `kingdomaward` names. `LadderGridTest::testParkOnlyGridUsesGlobalLadderColumns` and `ReportDomainCoverageTest::testLadderGridParkScopeAndAwardedPlayer` assert park-only grids use global award names even when the kingdom alias differs.

Commit: `c9656250`
