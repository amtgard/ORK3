# Pending PR reply — C-26 (comment id 3691046669)

Post on: https://github.com/amtgard/ORK3/pull/492#discussion_r3691046669

---

Fixed in `fix-pr-492`: `QualTestAjax/getreports` again returns both `counts` and `reporters`. Added `Model_QualTest::report_details()` delegating to `QualTest::getReportDetails()` so the question-reports UI can populate `renderReporters`. `QualTestGetReportsTest` locks the payload shape for a flagged question; `QualTestScoreTest` asserts the model wrapper matches the domain method.

Commit: `56a3220a`
