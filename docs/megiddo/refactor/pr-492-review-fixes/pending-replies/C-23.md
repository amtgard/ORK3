# Pending PR reply — C-23 (comment id 3691046651)

Post on: https://github.com/amtgard/ORK3/pull/492#discussion_r3691046651

---

Fixed in `fix-pr-492`: `GetServerHealthWeatherSummary` now uses the same Token + global `AUTH_ADMIN` gate as `GetServerHealthDbStatus` and `GetServerHealthProcesses` (C-06). `Model_AdminDashboard::server_health_weather_summary()` and the admin server-health controller pass the session token; `Weather::GetFreshnessBuckets()` forwards an optional token (no Json allowlist entry). `ServerHealthStatsTest` asserts stranger/empty token denied and admin token succeeds.
