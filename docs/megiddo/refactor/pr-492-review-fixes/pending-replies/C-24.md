# Pending PR reply — C-24 (comment id 3691046657)

Post on: https://github.com/amtgard/ORK3/pull/492#discussion_r3691046657

---

Fixed in `fix-pr-492`: `GetServerHealthDbStatus` whitelists each `$wanted` entry against `ListServerHealthDbStatusVariables()` / `SERVER_HEALTH_DB_STATUS_VARIABLES` (same server-health subset as the admin panel) before building the `SHOW GLOBAL STATUS … IN (...)` query; unknown and injection-shaped names are dropped (empty whitelist → empty result). `ServerHealthStatsTest` covers injection-only / mixed payloads and legitimate `Uptime` + `Threads_connected` with admin Token.
