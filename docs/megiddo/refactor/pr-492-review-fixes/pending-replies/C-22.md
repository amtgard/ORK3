# Pending PR reply — C-22 (comment id 3691046645)

Post on: https://github.com/amtgard/ORK3/pull/492#discussion_r3691046645

---

Fixed in `fix-pr-492`: the five new Report getters now require a session `Token` and restore controller-equivalent gates:

- `GetAdminDashboardStats` — global `AUTH_ADMIN` (same as `Administration::PurgeLogs` / C-06 health getters).
- `GetVotingEligibleForPlayer` — self or park `CREATE` / kingdom `EDIT` / admin (aligned with `SetPlayerActiveStatus`).
- `GetAttendanceDates`, `GetKingdomOfficerDirectoryMerged` (scoped kingdom), `GetLadderAwardGrid` — park `CREATE` or kingdom `EDIT`, or global admin; all-kingdom officer directory requires global admin.

`GetVotingEligible` at ~3593 is unchanged.

`Model_AdminDashboard` and `Model_Reports` pass the session token; integration/unit tests cover admin vs stranger for dashboard stats and scoped denial/success for attendance dates.
