# Pending PR reply — C-21 (comment id 3691046642)

Post on: https://github.com/amtgard/ORK3/pull/492#discussion_r3691046642

---

Fixed in `fix-pr-492`: `Player::GetDisplayGrants`, `GetOfficerRoles`, `GetBeltlineForPlayer`, and `GetRevokedAwardsForPlayer` now require a session `Token` and gate on the same profile-editor surface as `controller.Player` (self, park/kingdom `AUTH_EDIT` on the subject’s scope, or global `AUTH_ADMIN`).

`Model_Player` wrappers pass the session token and map auth failures to empty UI-safe shapes where the controller already gates visibility.

Integration tests: anonymous → `SecureTokenFailure`; stranger → `NoAuthorization`; self and park editor paths succeed (`GetDisplayGrants`, `GetRevokedAwardsForPlayer`, plus shared gate coverage for the other two getters).
