# Pending PR reply — C-29 (comment id 3691046691)

Post on: https://github.com/amtgard/ORK3/pull/492#discussion_r3691046691

---

`Weather::coords_for_calendar_detail` now treats either zero latitude or zero longitude as missing coordinates (same sentinel rule as park scalar coords before blob fallback), so half-populated rows no longer reach Open-Meteo at lng 0. `WeatherServiceTest::testCoordsForCalendarDetailRejectsHalfZeroSentinels` covers both-zero, one-axis-zero, and valid lat/lng pairs.

Commit: `f6cf455c`
