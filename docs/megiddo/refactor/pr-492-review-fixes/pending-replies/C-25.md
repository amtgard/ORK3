# Pending PR reply — C-25 (comment id 3691046664)

Post on: https://github.com/amtgard/ORK3/pull/492#discussion_r3691046664

---

Fixed in `fix-pr-492`: `AddAttendance` only calls `reactivateInactiveMundane` when the request includes truthy `ReactivateInactive` (set by `Model_Attendance::add_attendance`, the park `AttendanceAjax` park/add path). Other callers (event sheet, kingdom/park forms, SOAP/import) no longer auto-reactivate inactive players. Reactivation writes a `danger_audit` row via `Attendance::ReactivateInactiveMundane` with parameters including `by_whom_id`, prior/post `active` state, and audit `by_whom_id` from the authorizing officer token. `AttendanceSignInTest` covers no-flag vs flag + audit; park-add model path asserts `Reactivated`.
